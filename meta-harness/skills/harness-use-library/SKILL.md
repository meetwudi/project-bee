---
name: harness-use-library
description: Resolve and use Meta Harness Libraries in the current repository. Use when the user provides a library:// reference, asks to find relevant project knowledge, needs task memory or compliance context, or wants Codex to discover governed knowledge through harness/libraries indexes and a Library's own AGENTS.md.
---

# Harness: Find and Use Library

Use this skill to resolve `library://` references and explore governed knowledge.

1. Read `meta-harness/setup/PRIMITIVE-ORIENTATION.md`.
2. Read `meta-harness/primitives/LIBRARY.md`.
3. Follow the repository `AGENTS.md` chain to Library discovery.
4. Resolve requested or relevant Libraries through the checked-in and local Library indexes.
5. Explore each selected Library through its own `AGENTS.md` and governance.
6. Return or use only the knowledge needed for the current task.

If a `library://` reference cannot be resolved, report the missing Library name and the indexes checked.
